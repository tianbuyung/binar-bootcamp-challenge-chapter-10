const getUser = (req, res) => {
  res.json({
    message: "Get User",
  });
};

const getUserAllUser = (req, res) => {
  res.json({
    message: "Get User",
  });
};

module.exports = { getUser, getUserAllUser };
